
Anniversary React Site
----------------------

How to run:
1. unzip this folder to your machine
2. open terminal in project root
3. run: npm install
4. run: npm start
If you want Tailwind features fully, install tailwind dependencies:
  npm install -D tailwindcss postcss autoprefixer
  npx tailwindcss init -p
But the project includes minimal tailwind setup files.
